## 1. Extract shared supervisor primitives

- [x] 1.1 Create a shared, Electron-agnostic `supervisor/` package at repo root; move `descriptors.js`, `process.js`, `health.js`, `logs.js`, `ports.js`, and the `Supervisor` class (from `electron/supervisor/lifecycle.js`) into it, removing any Electron-only imports.
- [x] 1.2 Confirm `getResourceRoot` / `getDescriptors` are fully parameterised by `projectRoot` (dev falls back to `path.join(projectRoot, "resources")`, packaged uses `process.resourcesPath`) with no other Electron coupling.
- [x] 1.3 Rewire `electron/supervisor/` to re-export the shared module and update `electron/main.js` imports so the packaged supervisor still constructs `Supervisor` with bundle-resolved `nodeBin`/`projectRoot`/`dataDir`/`agentEnv`.
- [x] 1.4 Run `npm run test:e2e:smoke` and verify the packaged supervisor's bundled-vs-external resolution, startup/shutdown ordering, and restart-on-crash behave exactly as before. (Shared `Supervisor` import + re-export chain verified; exercised end-to-end via the launcher in local/external/absent modes incl. ordered SIGINT shutdown and optional-non-blocking. `serverPort` + optional-non-blocking changes are backward-compatible. A full packaged Electron GUI run isn't possible headless.)

## 2. Extract shared first-run seeder

- [x] 2.1 Extract `electron/first-run.js`'s seeding (atomic copy `resources/litellm/default-config.yaml` -> `<dataDir>/litellm.yaml`; generate `LITELLM_API_KEY` (`sk-`+32-byte hex) and `OPENCONNECTOR_RUNTIME_TOKEN`/`ADMIN_TOKEN` (32-byte hex) when absent; idempotent atomic writes) into an Electron-agnostic helper that takes `dataDir` + a settings-store path instead of `app.getPath("userData")`.
- [x] 2.2 Rewire `electron/first-run.js` to delegate to the shared helper; verify the existing `first-run-bootstrap` scenarios are unchanged.
- [x] 2.3 Confirm tokens are still injected only into child env (never sent to renderer) after the extraction.

## 3. Headless local-services launcher

- [x] 3.1 Create `local-services.js`: resolve dev env (load `.env` via dotenv, merge dev-settings file), compute `agentEnv`, `nodeBin = process.execPath`, `dataDir = process.env.PLATFORM_DATA_DIR || projectRoot`.
- [x] 3.2 In the launcher, run the shared seeder once before `Supervisor.start()` when bundled resources are present.
- [x] 3.3 Construct the shared `Supervisor` (server-js + litellm + openconnector; pi-agent stays `enabled: false`), call `start()`, await `server-js` healthy, and log a one-line per-service summary (local-spawned / external / absent).
- [x] 3.4 Wire SIGINT/SIGTERM to `supervisor.stop()` for ordered shutdown without triggering restart-on-crash.
- [x] 3.5 Add `scripts/start.js` as the thin entry that imports and runs `local-services.js`.

## 4. Entry point, gitignore, and docs

- [x] 4.1 Change `package.json` `start` from `node server.js` to `node scripts/start.js`.
- [x] 4.2 Add the dev-settings file and the CWD-relative data dir to `.gitignore` so generated credentials are not committed.
- [x] 4.3 Update `CLAUDE.md` (Configuration + Commands) and `README.md` to document the local-services dev workflow: build resources first, clear `LITELLM_BASE_URL`/`OPENCONNECTOR_BASE_URL` from `.env` to go local, keep them to stay remote.

## 5. Validation

- [x] 5.1 With `.env` URLs cleared: `npm start` spawns local LiteLLM + OpenConnector + `server.js`; chat works; `/oc-web` and `/litellm-web` load; the model selector lists LiteLLM models.
- [x] 5.2 With `.env` external URLs set: `npm start` uses the remote servers and spawns no local LiteLLM/OpenConnector.
- [x] 5.3 With bundled resources absent (simulated by temporarily renaming `resources/`): `npm start` degrades to `server.js`-only with a clear warning and no crash.
- [x] 5.4 Run `npm run test:e2e:smoke` to confirm no regression across the change. (37/38 pass: all 36 fast + session-resume smoke. The 1 failing smoke test is the real-LLM chat-turn test, which fails because the `.env`-configured remote LiteLLM 192.168.1.4:4000 is unreachable from this host - pre-existing/environmental, not a regression: e2e runs `node server.js` directly with unchanged `.env`.)

## 6. Local-mode runtime fixes (discovered during first real local run)

- [x] 6.1 Launcher dotenv `override: true` so the project `.env` wins over shell-exported vars (a globally-exported `LITELLM_BASE_URL` from `.zshrc` was forcing external mode despite `.env` clearing it).
- [x] 6.2 Fix LiteLLM config env-var syntax: `$VAR` -> `os.environ/VAR` in `resources/litellm/default-config.yaml` (template) and the seeded `litellm.yaml`. LiteLLM does not substitute `$VAR` - it was passing the literal string, causing `Connection error` to Volces.
- [x] 6.3 `.env` switched to local mode: remote URLs/tokens emptied (so override clears them), `VOLCES_API_KEY` set to the key with available quota (`ark-e3597b3f-…`) and `VOLCES_BASE_URL` set (the other key `ark-24959dea-…` is out of monthly quota until 2026-08-09).
- [x] 6.4 Verified end-to-end: `npm start` spawns local LiteLLM + OpenConnector + `server.js` (all healthy); chat through local LiteLLM -> Volces returns real completions (`"Hello!"`); local OpenConnector `/v1/health` -> 200.
- [x] 6.5 Make the project's internal services explicit in `.env`: a localhost `*_BASE_URL` (e.g. `LITELLM_BASE_URL=http://localhost:4000`, `OPENCONNECTOR_BASE_URL=http://localhost:3001`) now makes the launcher spawn the bundled service on that exact port (was: only empty/unset triggered local spawn, on a random port). Supervisor accepts `litellmPort`/`ocPort`; launcher parses the port from the localhost URL. Verified: litellm `:4000` chat -> Volces `"Hello! How can I assist you today?"`; OC `:3001` `/v1/health` -> 200. Updated CLAUDE.md, README, and the `local-services-launcher` spec.
- [x] 6.6 Cross-platform bundled resources (mac arm64 + win x64) so the `.exe` also runs local LiteLLM/OpenConnector. `supervisor/descriptors.js` resolves platform-correct python/venv paths (`python/bin/python3`|`python/python.exe`, `venv/bin/litellm`|`venv/Scripts/litellm.exe`) and normalizes the OC `sqlite://` URL backslashes for Windows. `scripts/build-python-litellm.sh` (mac-only bash) replaced by `scripts/build-python-litellm.js` (cross-platform Node: picks the right python-build-standalone asset, uses `curl`+`tar` available on win10+/mac). `scripts/verify-bundle.js` checks the platform-correct paths and requires them on mac arm64 OR win x64. `package.json` `predist` uses the `.js`. Verified mac regression: build skips, verify passes, `npm start` local litellm `:4000` chat `"OK"` + OC `:3001` 200. (Windows `.exe` build itself needs a Windows host to run `predist`+`dist`.)
- [x] 6.7 Fix OpenConnector MCP startup race so pi actually gets OC's tools. Root cause: server.js was spawned+awaited first, so its `initAgent()` connected OC's `/mcp` before OC was spawned -> tools skipped (`mcpToolCount` was 9 = memory only). Fix: (a) `supervisor/lifecycle.js` spawns bundled litellm/OC sidecars fire-and-forget BEFORE server.js so they warm up in parallel; (b) `mcp-bridge.js` `connectServers` supports retry; (c) `server.js` retries the OC MCP connect (~30s) until OC is ready. Verified: `mcpToolCount` now 14 (9 memory + 5 OC: list_apps/search_actions/get_action_guide/execute_action/+1), LiteLLM provider active (`deepseek-v4-pro`). e2e: OC disabled in `playwright.config.js` (no real OC in e2e) -> 36/36 fast pass in 24.8s.
- [x] 6.8 Fix "can't open LiteLLM" from the app/web. Root cause: the local bundled LiteLLM management UI (`/ui`) requires the master key to sign in, but `LITELLM_API_KEY` is auto-generated server-side so the user had no credentials. Fix: (a) `server.js` `/api/litellm/credentials` returns the master key ONLY when LiteLLM is local (localhost) - null for remote (user has own creds); (b) the LiteLLM React page fetches + shows the master key (copyable) next to the "Open dashboard" link; (c) `electron/main.js` `setWindowOpenHandler` opens external http(s) links (the dashboard) in the system browser so `target="_blank"` works in the packaged app. Verified: `/api/litellm/credentials` returns the key in local mode; web built; e2e 36/36.
- [x] 6.9 Fresh-install self-containment. (a) `.env.example` committed (local-mode defaults: localhost URLs + `VOLCES_API_KEY` placeholder + note that LITELLM_API_KEY/OC tokens are auto-generated) - `cp .env.example .env` on a fresh clone; (b) `scripts/postinstall-bundle.js` builds the bundled LiteLLM+OC resources on `npm install` if missing (best-effort, gated by `PLATFORM_SKIP_BUNDLE=1`, skips when already built, never fails the install); (c) `local-services.js` prints a `⚠️ Run npm run predist` hint when a service is configured local but its bundled resources are absent. Verified: postinstall skips when built, `.env.example` not gitignored, hint fires on simulated-absent.
- [x] 6.10 Fix sidecar startup regression from 6.7: the main startup loop skipped ALL litellm/oc sidecars, leaving http-external/disabled ones stuck in "pending" (external mode + absent mode both broken). Fix: track spawned sidecars in a Set and skip only those; http-external/disabled sidecars now flow through the main loop (probed / marked disabled). Verified: absent mode shows `absent (disabled)` + hint; local mode `local (healthy)` + `mcpToolCount 14` + chat `"OK"`; e2e 36/36.
- [x] 6.11 Avoid port conflicts with the user's other services. The packaged app already uses random free ports (no conflict). The dev launcher fixed 3000/4000/3001 which could clash: LiteLLM/OC now PREFER the port parsed from the .env URL but fall back to a free port (`resolvePort` + `isPortFree`) if it's in use (logs the swap; server.js gets the actual URL either way). server.js pins to PORT (default 3000 - needed by the Vite dev proxy + WS client) and errors clearly if 3000 is taken. Verified: normal -> 4000/3001; :4000 occupied -> litellm falls back to a free port (healthy); :3000 occupied -> clear error + exit 1. Updated `.env`/`.env.example` comments.
- [x] 6.12 Build the OpenConnector web console + provider catalog/registry so the OC tab's management UI works on first launch. Root causes + fixes in `scripts/build-openconnector.js`: (a) `copyDir` skipped ANY dir named `web` (incl. the built `dist/web`) - fixed to skip only the top-level `web/` workspace source (depth-aware); (b) added `npm run build:web` + `copy-catalog-assets` (vite `outDir: ../dist/web` -> `dist/web`); (c) the OC server also needs `catalog/apps/` + `src/providers/registry.generated.ts` - `ensure-generated.ts`'s mtime-based `catalogFresh` branch skips generators in the bundle, so generate them directly via plain `node scripts/generate-catalog.ts` (generates BOTH catalog + registry; tsx breaks the generator's path resolution, plain node works). Verified: `dist/web/index.html` + `registry.generated.ts` + 1097 catalog apps present; OC `local (healthy)`; `/oc-web` returns `<!doctype html>` (the console, was the "build:web" message); `mcpToolCount 14`.
